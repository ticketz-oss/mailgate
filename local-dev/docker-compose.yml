services:
    redis:
        image: redis:7-alpine
        container_name: mailgate-redis-dev
        restart: unless-stopped
        ports:
            - '6380:6379'
        volumes:
            - redis_data:/data

volumes:
    redis_data:
